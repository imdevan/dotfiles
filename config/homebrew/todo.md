# TODO: 
- [ ] fresh install on new machine to test best method of brew install 
- - brew bundle --file=Brewfile
- - or 
- - xargs brew install < packages.txt
- [ ] clean up brew packages and file (https://docs.brew.sh/Brew-Bundle-and-Brewfile#brew-bundle-cleanup)

