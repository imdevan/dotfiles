# Empty sketchybar

PLUGIN_DIR="$CONFIG_DIR/plugins"

##### Bar Appearance #####
bar=(
  position=top
  height=0
  color=0x00000000
  border_width=0
  shadow.drawing=off
)
sketchybar --bar "${bar[@]}"

##### Force all scripts to run the first time (never do this in a script) #####
sketchybar --update
